function calc_supplyDemand (population,radius,fields,lifestyle)
   -- Mockup of interaction with planetary attributes.
   -- Scripts are passed a planet to run on, and poll that planet for attributes.
   -- The values of the attributes can be used in calculations. 
   --
   -- pollution = p:attribute("Pollution")
   -- crops = p:attribute("Bumper Crops")
   -- famine = p:attribute("Famine")
   -- supply = supply - 0.5 * pollution
   -- supply = supply + 2 * crops
   -- demand = 2 -- All planets always consume food.
   -- demand = demand + famine * 1.5
   -- return supply, demand

   -- Test values.
   return 2, 0
end
